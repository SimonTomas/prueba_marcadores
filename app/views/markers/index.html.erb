<p id="notice"><%= notice %></p>

<h1 class="text-center">Marcadores</h1>

<%= link_to 'Administrar Categorías', categories_path, class: 'btn btn-primary' %>
<%= link_to 'Administrar Tipos', types_path, class: 'btn btn-primary' %>

<%= render 'form' %>

<table class="table table-striped mt-3">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Direccion URL</th>
      <th scope="col">Tipo</th>
      <th scope="col">Categoria</th>
      <th colspan="3" scope="col"></th>
    </tr>
  </thead>

  <tbody id="tbody">
      <%= render @markers %>
  </tbody>
</table>